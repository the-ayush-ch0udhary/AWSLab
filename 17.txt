Write a program using UDP socket such that a client will accept a message from the
user and send it to the server. The server will display the message and the IP address
and port number of the client.

server

#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <arpa/inet.h>
#include <sys/socket.h>

#define SERVER_IP "127.0.0.1"
#define SERVER_PORT 5007

int main(){
    int sd, len;
    struct sockaddr_in server, client;
    char msg[512];

    sd = socket(AF_INET, SOCK_DGRAM, 0);

    bzero((char *)&server, sizeof(server));
    server.sin_family = AF_INET;
    server.sin_addr.s_addr = inet_addr(SERVER_IP);
    server.sin_port = htons(SERVER_PORT);

    bind(sd, (struct sockaddr *)&server, sizeof(server));

    len = sizeof(client);

    recvfrom(sd, msg, 512, 0, (struct sockaddr *)&client, &len);

    printf("Message from client: %s\n", msg);

    // IMPORTANT PART
    printf("Client IP: %s\n", inet_ntoa(client.sin_addr));
    printf("Client Port: %d\n", ntohs(client.sin_port));

    close(sd);
    return 0;
}

client

#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <arpa/inet.h>

#define SERVER_IP "127.0.0.1"
#define SERVER_PORT 5007

int main(){
    int sd;
    struct sockaddr_in server;
    char msg[512];

    sd = socket(AF_INET, SOCK_DGRAM, 0);

    bzero((char *)&server, sizeof(server));
    server.sin_family = AF_INET;
    server.sin_addr.s_addr = inet_addr(SERVER_IP);
    server.sin_port = htons(SERVER_PORT);

    printf("Enter message: ");
    scanf("%s", msg);

    sendto(sd, msg, strlen(msg)+1, 0, (struct sockaddr *)&server, sizeof(server));

    close(sd);
    return 0;
}